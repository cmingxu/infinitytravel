<%= form_for([:admin, @brochure_travel],:html => {:class => "form-horizontal" }) do |f| %>
  <% if @brochure_travel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@brochure_travel.errors.count, "error") %> prohibited this brochure_travel from being saved:</h2>

      <ul>
      <% @brochure_travel.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="control-group">
    <label class="control-label">brochure_id</label>
    <div class="controls">
    <%= f.text_field :brochure_id %>
    </div>
  </div>
  <div class="control-group">
    <label class="control-label">travel_id</label>
    <div class="controls">
    <%= f.text_field :travel_id %>
    </div>
  </div>
  <div class="control-group">
    <div class="controls">
      <%= f.submit %>
    </div>
  </div>
<% end %>